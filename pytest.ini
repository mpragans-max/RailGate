[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
asyncio_default_fixture_loop_scope = function
markers =
    xray: requires a real Xray-core binary on the system
filterwarnings =
    ignore::DeprecationWarning
