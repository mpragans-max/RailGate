# RailGate runtime dependencies — pinned for deterministic builds.
# Update deliberately, then re-run `pytest` and rebuild the image.

fastapi==0.115.6
starlette==0.41.3
uvicorn==0.34.0
uvloop==0.21.0
httptools==0.6.4
websockets==14.1
jinja2==3.1.5
python-multipart==0.0.20
argon2-cffi==23.1.0
qrcode==8.0
httpx==0.28.1
python-dotenv==1.0.1
